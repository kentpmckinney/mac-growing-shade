import { Message, SEND_MESSAGE, DELETE_MESSAGE, ChatActionTypes } from '../state/types';
import { SystemState, UPDATE_SESSION, SystemActionTypes, ChatState } from '../state/types'

function increment() {
    return { type: 1 }
}