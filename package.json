{
  "name": "growing-shade",
  "version": "0.2.0",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm intall --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^6.0.2"
  }
}
